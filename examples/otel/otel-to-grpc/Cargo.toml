[package]
name = "otel-to-grpc"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
hex = "0.4"
opentelemetry-proto = { version = "0.29", default-features = false, features = ["logs", "gen-tonic-messages"] }
prost = "0.13"
wit-bindgen = "0.52"
